<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tienda</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRk2vvoC2f3B09zVXn8CA5QIVfZOJ3BCsw2P0p/We" crossorigin="anonymous">
    <link rel="stylesheet" href="stylesheets/tienda.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lobster&display=swap" rel="stylesheet">
</head>

  <body>

    
<nav class="navbar navbar-expand-lg navbar-dark bg-dark ">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Victoria commerce</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/contacto">Contacto</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/tienda" tabindex="-1">Tienda</a>
          </li>
        </ul>
        <form class="d-flex">
          <input class="form-control me-2" type="search" name="q" {{#if q}}value="{{q}}"{{/if}} placeholder="Victoria commerce" aria-label="Search">
          <button class="btn btn-outline-success" onclick= res.redirect('tienda') type="submit">Buscar</button>
        </form>
      </div>
    </div>
  </nav>

<h1>Productos</h1>


<div class="productos">{{#each productos}}
  <div class="producto">
    <div class="imagen">
    <img src="{{imagen}}" width='250' height='300'> </img>
    </div>
    <div class="titulo">
    {{titulo}}
    </div>
    <div class="subtitulo">
    {{subtitulo}}
    </div>
    <div class="cuerpo">
    {{cuerpo}}
  </div>
  <div class='precio'>
      <p>$ {{precio}}</p>
  </div>
</div>
{{/each}}

{{#if is_search}}
  {{#unless productos.length}}
    No se encontraron resultados para "<strong>{{q}}</strong>"
  {{/unless}}
{{/if}}


</div>